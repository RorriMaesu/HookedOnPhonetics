rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Development mode - comment out for production
    match /{allPaths=**} {
      allow read, write: if true;
    }
    
    // Production rules - uncomment for production
    /*
    // Allow authenticated users to read and write only their own files
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Allow authenticated users to read shared resources
    match /resources/{resourceId} {
      allow read: if request.auth != null;
      allow write: if false; // Only admins can write (handled through Firebase Admin SDK)
    }
    
    // Allow authenticated users to read shared images
    match /images/{imageId} {
      allow read: if request.auth != null;
      allow write: if false; // Only admins can write (handled through Firebase Admin SDK)
    }
    
    // Allow authenticated users to read shared audio
    match /audio/{audioId} {
      allow read: if request.auth != null;
      allow write: if false; // Only admins can write (handled through Firebase Admin SDK)
    }
    
    // Admin users can read and write all files
    match /{allPaths=**} {
      allow read, write: if request.auth != null && 
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    */
  }
}
